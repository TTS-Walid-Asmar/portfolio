<%= form_for(@govserv) do |f| %>
  <% if @govserv.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@govserv.errors.count, "error") %> prohibited this govserv from being saved:</h2>

      <ul>
      <% @govserv.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :service %><br>
    <%= f.text_area :service %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
