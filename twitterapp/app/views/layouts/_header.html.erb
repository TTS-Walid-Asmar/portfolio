<% if user_signed_in? %>
<nav class="navbar navbar-default">
<div class="container-fluid">
<!-- Brand and toggle get grouped for better mobile display -->
<div class="navbar-header">
   <a class="navbar-brand"> <%= link_to image_tag(asset_path "twitterhome.png"), feed_path, class: "navbar-brand" %>
  </a>
       </div>

<!-- Collect the nav links, forms, and other content for toggling -->
<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

  <ul class="nav navbar-nav navbar-left">
      <!--Future links here-->
    </ul>

  <ul class="nav navbar-nav navbar-right">
      <%= form_tag search_tweets_path, method: :get, class: "navbar-form navbar-right", role: "search" do %>
    <p>
      <%= text_field_tag :search, params[:search], class: "form-control" %>
      <%= submit_tag "Search", name: nil, class: "btn btn-default" %>
    </p>
  <% end %>
      <li class="dropdown">
      <a class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><div class="navpic"><%= image_tag(current_user.avatar.url(:navpic)) %></div></a>
      <ul class="dropdown-menu" role="menu">
          <li><%= link_to current_user.username, user_profile_path(id: current_user.id) %>
<!--                  <span class="tiny text"><%= link_to "View Profile", user_profile_path(id: current_user.id) %></span>-->
          </li>
            <li class="divider"></li>
             <li><a href="#">Something else here</a></li>

        <li class="divider"></li>
        <li><a href="#">One more separated link</a></li>
                      <li class="divider"></li>

        <li><%= link_to "Edit Profile", edit_user_registration_path %></li>
       <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
      </ul>
    </li>
      <li><div class="btn-group">
          <div class="navbtn"><%= button_to "Tweet", @create, remote: true %>
<!--    <%= link_to 'Tweet', new_path, class: "btn btn-info" %>-->
          </div>
</div></li>
  </ul>
</div>
</div>
    </nav>
    <% else %>
    <nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
             <span class="icon-bar"></span>

            </button>
  <ul class="nav navbar-nav navbar-right">
            <li><%= link_to "Sign Up", new_user_registration_path %></li>
            <li><%= link_to "Forgot Password", new_user_session_path %></li>
            </ul>
        </div>
        </div>
    </nav>
    <% end %>
